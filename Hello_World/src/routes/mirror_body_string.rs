// here in the code the fn arguments: body for the raw string it takes the whole thing sent
pub async fn mirror_body(body: String) -> String {
    println!("logging from mirror body....");
    body
}
